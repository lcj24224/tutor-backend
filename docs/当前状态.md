# 当前状态

> AI 摘要:项目工作上下文总入口,AI 会话启动必读 | 何时读:每次会话启动、每次阶段验收后刷新 | 更新:2026-09-09

## 分支与提交

- 分支:master(后端 ruoyi-vue-pro);工作区存在大量未提交改动(PG 切换、设计文档 v0.3、SQL 脚本清理、文档体系改造,以 `git status` 实时结果为准)

## 当前阶段:阶段 0 完成 ✅,阶段 1 待启动

- ✅ 阶段 0-1:数据库切换 PostgreSQL 16.14 + pgvector(免安装版 `E:\pgsql`),11 张业务表建库验证
- ✅ 阶段 0-2:设计文档定稿 v0.3(产品/数据库/字段字典/接口/规范/底座指南)
- ✅ 阶段 0-3:文档体系改造(文档索引 + 接口按模块拆分 + AI 双读者改造 + 随仓库独立)
- ✅ 阶段 0-4:**yudao-module-tutor 模块骨架建成**(2026-09-09:包结构/依赖/注册/编译/启动全部验证通过)
- ⏭️ 下一步:**阶段 1 竖切核心链路**:微信登录 → 用户体系 → 老师认证 → 简历 → 家长需求 → 投递 → 选中 → 订单生成 → 老师确认 → 待收费

## 活跃任务

- 阶段 1 第一个接口:微信登录 `/app-api/auth/wechat-login`(复用 yudao OAuth2TokenService 签发 token,见接口文档 01)
- 合规并行项(用户线下):正式小程序注册(备案已完成)、logo、OSS 开通、腾讯地图 key 确认、隐私政策文案、订阅消息模板申请
- 待确认:信息费定价 99 元为模拟 v0.1,正式定价待用户定稿

## 各模块差异摘要

业务代码未动工,无代码/文档差异。开发启动后,每个模块的"需求 vs 实现"差异按 `docs/接口/` 对应模块文档维护,超期差异汇总回本文件。

## 最近决策

见 `docs/决策日志.md`。最新条目:2026-09-09 文档体系 AI 双读者改造、鉴权二开决策、信息费定价模拟 v0.1、三身份并存等(全部已落文档 v0.3)。

## 待裁定事项

1. 信息费正式定价(当前 99 元为模拟 v0.1)
2. 订阅消息 4 个模板的文案与申请(需正式小程序账号)
3. 客服官方会话(open-type=contact)的客服人员配置

## 归档原则

旧快照、已完成计划、历史审计集中到 `docs/归档/`(届时建立),只保留证据,不作为执行依据。
