# 决策日志

> AI 摘要:历史决策与经验教训,同类问题先查这里再决定是否重新讨论 | 何时读:综合分析前必查、复盘时追加 | 更新:2026-09-09

## 使用规则

- 每次方案拍板后追加一条,格式固定:**日期 / 主题 / 决策 / 理由 / 参与方 / 落地状态**
- 遇到同类问题先查本文件——已有结论直接采用,不再重复讨论
- 参与方标注:用户裁定(用户拍板)/ 用户+AI 讨论(共同确认)/ AI 建议用户采纳

## 决策记录

### 2026-09-09 / 数据库选型:MySQL → PostgreSQL 16 + pgvector

- **决策**:后端数据库由 MySQL 切换为 PostgreSQL 16(免安装版装于 `E:\pgsql`,postgres/123456),启用 pgvector 0.8.6 扩展;官方 SQL 与业务 SQL 均无需修改(yudao 官方已提供 PG 版脚本,业务脚本 v0.2 起已是 PG 语法)
- **理由**:数据库设计文档 v0.2 既定(PG 学习目的 + 语义检索一步到位)
- **参与方**:用户裁定,AI 实施
- **落地状态**:已完成并验证(框架 49 表 + quartz 11 + 业务 11 表 + 字典 33 条;编译/启动/API 全通过)

### 2026-09-09 / 表设计评审 v0.3(12 项逐条拍板)

- **决策**:
  1. t_order 状态机重构:6 状态(0 待确认/1 待收费/2 试课中/3 已完成/4 试课失败/5 已取消)+ fee_status/fee_collected_at/fee_refunded_at/fail_blame(责任方审计)
  2. 信息费前置:老师确认后才收费,试课失败/收费后取消必退费(fee_status=2 强制)
  3. 管理端订单权限:客服可取消订单(家长放弃,收费后先退费)、标记失败必填责任方+原因
  4. t_application 撤回=终态不可重投(部分唯一索引 WHERE status IN (0,1,2,3,4))
  5. t_audit_log 加 operator_id(多客服追溯)
  6. t_dict 删除,字典复用 yudao system_dict_data(id 1000001 起)
  7. 认证材料只留学生证+学信网,个人照片取消(头像选填)
  8. t_user 加 mobile_hash(统一盐 SHA-256,客服按手机号查找)
  9. 下架语义改"可见性":visible 纯开关,编辑敏感字段→重审→通过恢复可见;t_demand 加 review_status/edit_version
  10. 站内信做单向通知(复用 yudao notify),用户间禁互发
- **理由**:AI 评审意见,用户逐条拍板或修正(详见对话)
- **参与方**:用户+AI 讨论
- **落地状态**:schema/字典/文档 v0.3 全部落地并重建验证

### 2026-09-09 / 身份体系:三身份并存 + 单端双视图

- **决策**:游客/老师/家长三身份并存(审核解锁,可同时发简历和需求);单小程序双视图,个人页视图切换;首次进入弹身份选择;身份由 profile 表推断不加字段;自投自抢禁止(投递校验 parent_user_id ≠ 自己);身份级处罚不连坐,用户封禁全封
- **理由**:用户拍板;大学生双身份场景真实,冷启动期用户池复用
- **参与方**:用户裁定
- **落地状态**:产品/数据库文档 v0.3 已落

### 2026-09-09 / 信息费定价:统一 99 元(模拟 v0.1)

- **决策**:99 元/单统一价,收费前置,试课失败/家长放弃/老师放鸽子一律全额退;话术"99 元介绍一位合适的家教老师,试课不满意全额退";演进:99→学段分档→比例制;订单表暂不存金额
- **理由**:AI 检索市场行情(聊城 200-500 固定/神舟 20% 押金月结/陪读 10% 试课后收),低锚点抵消前置收费抵触
- **参与方**:用户委托模拟,AI 出方案
- **落地状态**:产品文档 §13;**正式定价待用户定稿**

### 2026-09-09 / 鉴权体系:二开不自研

- **决策**:admin-api 复用 yudao OAuth2+RBAC 零改动;app-api 自建微信登录接口(复用 OAuth2TokenService 签发 token);业务权限(cert_status/blacklist/发布权/自我成交)写在 service 层,不进 RBAC
- **理由**:yudao token 基础设施(过滤器/表/Redis 会话/刷新/踢人)现成且久经验证,自研=重复造更差的轮子
- **参与方**:用户确认
- **落地状态**:接口文档 00 章 + CLAUDE.md 已落

### 2026-09-09 / 编辑重审粒度与需求审核方式

- **决策**:简历/需求仅**敏感字段**编辑触发重审(简历:学校/学历/擅长学科;需求:科目/年级/地址/时薪);非敏感字段走敏感词检测(内置开源词库+人工兜底)不重审
- **理由**:客服单人,全量重审会爆炸
- **参与方**:用户采纳 AI 建议
- **落地状态**:文档 v0.3 已落

### 2026-09-09 / 技术栈:JDK 17 运行、SB 2.7.18 锁定、不上 JDK 21

- **决策**:写作标准 JDK 8(编译目标 1.8)+ 引擎 JDK 17(本机 17.0.12 已验证);禁止自行改 spring.boot.version 升 3.x(被 yudao jdk8 分支生态锁定);JDK 21 虚拟线程/新语法在当前组合下无法兑现,不上
- **理由**:yudao master 默认分支为兼容中国存量 JDK8 环境的最大公约数;升级路径=将来整体切 master-jdk17 分支
- **参与方**:用户+AI 讨论
- **落地状态**:CLAUDE.md 技术栈节已落

### 2026-09-09 / 模块禁用方式:注释 pom,不删代码(教训)

- **决策**:未启用模块(member/mp/pay/bpm/crm/erp/mes/wms/hrm/fms/iot/im/ai/mall/report/pms)**仅注释依赖禁用,目录永久保留**;AI 曾擅自物理删除 16 个模块目录,被用户纠正后已全部 git 恢复
- **理由**:用户明确"现在用不上≠将来一定用不上"(mp/member/ai/pms 存在未来可能性);恢复启用=取消注释+重编译
- **参与方**:用户裁定(AI 越权后纠正)
- **落地状态**:目录已恢复;规则入 CLAUDE.md + 长期记忆

### 2026-09-09 / 多租户关闭(bug 修复)

- **决策**:`yudao.tenant.enable: false`。业务表 t_* 无 tenant_id 列,租户开启会导致业务表查询注入租户条件报错
- **参与方**:AI 发现并修复,已向用户说明
- **落地状态**:application.yaml 已改,**不可改回 true**(除非业务表加 tenant_id 列)

### 2026-09-09 / SQL 脚本清理

- **决策**:`sql/` 仅保留 postgresql/(官方)+ tutor/(业务);删除 mysql/dm/highgo/kingbase/opengauss/oracle/sqlserver/db2/tools(用户原话"mysql 的脚本可以直接删掉"为授权依据)
- **参与方**:用户裁定
- **落地状态**:已删(未提交);git 历史可恢复

### 2026-09-09 / 文档体系:AI 双读者改造

- **决策**:借鉴占星项目(horoscope-backend/docs)与桌面《占卜》md 体系:① 文档索引(文档宪法);② 接口按模块拆分(00-07);③ yudao 底座使用指南;④ 当前状态(会话入口);⑤ 决策日志(本文件);⑥ 全部核心文档加"AI 摘要"文件头;⑦ 文档落地规则(落档先提示用户判断)
- **理由**:用户要求"方便人看也要方便 AI 阅读执行编码工作"
- **参与方**:用户裁定
- **落地状态**:已完成(2026-09-09)

### 2026-09-09 / 城市区数据与杂项拍板

- **决策**:城市/区走后端静态接口下发+前端缓存(不占小程序主包);举报订单冻结不加状态字段(客服人工跟进);客服体系 MVP 单人(用户本人);敏感词内置开源词库
- **参与方**:用户+AI 讨论
- **落地状态**:文档 v0.3 已落

### 2026-09-09 / 文档体系架构:取消全局层,各仓库文档独立

- **决策**:取消 tutor-miniapp 根的"全局文档"概念;docs 全部文档(产品设计/数据库设计/文档索引/当前状态/决策日志)与业务 SQL(01-schema/02-dict-data)迁入后端仓库(ruoyi-vue-pro/docs/ 与 sql/tutor/);**每个项目的文档体系随仓库独立**(小程序/管理后台开工时各自建立自己的文档)
- **理由**:用户指出"像数据库 SQL 脚本只执行后端项目的,应该在后端项目中,不应该在全局";文档随仓库走,变更与代码同提交,避免跨仓库契约漂移
- **参与方**:用户裁定
- **落地状态**:已完成迁移与全部引用更新(2026-09-09);此前"全局 docs+sql 不进 git"的风险随迁移消失(后端仓库已提交)

### 2026-09-09 / GitHub 代理与仓库推送

- **决策**:Git 配置 URL 级代理(github.com 走 Clash 7890);本地 master 跟踪 lcj24224/main;历史分叉采用强制推送覆盖(占位 README 已备份至 Temp)
- **参与方**:用户+AI 讨论
- **落地状态**:已完成;Clash 须保持运行
